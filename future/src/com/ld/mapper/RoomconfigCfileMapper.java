package com.ld.mapper;

import com.ld.model.RoomconfigCfile;

public interface RoomconfigCfileMapper extends BaseMapper<RoomconfigCfile>{
  //  int insert(RoomconfigCfile record);
   // int insertSelective(RoomconfigCfile record);
}